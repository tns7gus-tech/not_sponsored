[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt"
watchPatterns = ["services/api/**"]

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
numRetries = 10
